---
layout: simple.njk
---

<article class="article-content">
  {% if not published %}
  <div class="draft-notice">
    <strong>Draft:</strong> This article is not yet published and won't appear in the articles listing.
  </div>
  {% endif %}

  <div class="article-hero">
    <img src="{{ image or '/img/default-article.jpg' }}" alt="{{ title }}" class="article-hero-image">
    <div class="article-hero-overlay"></div>
    <div class="article-hero-content">
      <header class="article-header">
        <h1 class="article-title">{{ title }}</h1>
        {% if description %}
        <p class="article-description">{{ description }}</p>
        {% endif %}
      </header>
    </div>
  </div>

  <div class="article-body">
    {{ content | safe }}
  </div>

  <footer class="article-footer">
    <a href="/articles/" class="back-to-articles">← Back to Articles</a>
  </footer>
</article>

<style>
/* Override main container padding for full-bleed hero */
.main:has(.article-content) {
  padding-top: 0;
}

.article-content {
  max-width: 1200px;
}

.article-hero {
  position: relative;
  margin: 0 0 var(--space-xl) 0;
  overflow: hidden;
  min-height: 375px;
  /* Break out of container to full viewport width */
  width: 100vw;
  margin-left: calc(-50vw + 50%);
}

.article-hero-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  z-index: 1;
}

.article-hero-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(135deg, rgba(99, 102, 241, 0.15) 0%, rgba(79, 70, 229, 0.25) 100%);
  z-index: 2;
  pointer-events: none;
}

.article-hero-content {
  position: relative;
  z-index: 3;
  display: flex;
  justify-content: center;
  align-items: flex-end;
  min-height: 375px;
}

.article-header {
  width: 100%;
  padding: 20px 40px;
  background: rgba(30, 32, 38, 0.5);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: none;
  text-align: center;
}

.article-header h1 {
  margin-bottom: 1rem;
  color: white;
  font-size: 2.5rem;
  font-weight: 700;
  line-height: 1.2;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
}

.article-meta {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  padding: 0.75rem 1.5rem;
  margin-bottom: 1rem;
  font-size: 0.9rem;
  color: rgba(255, 255, 255, 0.85);
}

.article-meta time {
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.article-meta .lucide {
  width: 0.9rem;
  height: 0.9rem;
  color: rgba(255, 255, 255, 0.85);
}

.article-tags {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
  justify-content: center;
}

.tag {
  background: rgba(255, 255, 255, 0.15);
  padding: 0.3rem 0.6rem;
  border-radius: var(--radius-md);
  font-size: 0.8rem;
  color: white;
  border: 1px solid rgba(255, 255, 255, 0.25);
}

.article-description {
  font-size: 1.1rem;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.6;
  font-style: italic;
  margin: 0;
  text-align: center;
}

.article-body {
  line-height: 1.7;
}

.article-body h2 {
  margin-top: 2.5rem;
  margin-bottom: 1rem;
}

.article-body h3 {
  margin-top: 2rem;
  margin-bottom: 0.8rem;
}

.article-body p {
  margin-bottom: 1.5rem;
}

.article-github-cta {
  margin: var(--space-2xl) auto var(--space-xl);
  text-align: center;
}

.article-github-cta .btn-unified {
  width: auto;
  display: inline-flex;
}

.article-body ul, .article-body ol {
  margin-bottom: 1.5rem;
  padding-left: 2rem;
}

.article-body li {
  margin-bottom: 0.5rem;
}

.article-body blockquote {
  border-left: 4px solid var(--color-link);
  padding-left: 1.5rem;
  margin: 2rem 0;
  font-style: italic;
  color: var(--color-primary-light);
}

.article-body code {
  background: var(--color-border);
  padding: 0.2rem 0.4rem;
  border-radius: 3px;
  font-family: var(--font-family-monospace);
  font-size: 0.9rem;
}

.article-body pre {
  background: var(--color-border);
  padding: 1rem;
  border-radius: 6px;
  overflow-x: auto;
  margin: 1.5rem 0;
}

.article-body pre code {
  background: none;
  padding: 0;
}

.article-footer {
  border-top: 1px solid var(--color-border);
  padding-top: 2rem;
  margin-top: 3rem;
}

.back-to-articles {
  color: var(--color-link);
  text-decoration: none;
  font-weight: 500;
}

.back-to-articles:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}

.draft-notice {
  background: #fff3cd;
  border: 1px solid #ffeaa7;
  color: #856404;
  padding: 1rem;
  border-radius: 6px;
  margin-bottom: 2rem;
  font-size: 0.9rem;
}

/* Inline TL;DR paragraph style */
.article-body .tldr-banner {
  background: var(--color-surface-50);
  border-left: 4px solid var(--color-link);
  padding: 1rem 1.5rem;
  margin: 64px 0;
  border-radius: var(--radius-md);
}

.article-body .tldr-banner strong {
  color: var(--color-link);
  font-weight: 700;
  margin-right: 0.5rem;
}

.article-body .tldr-banner p {
  margin: 0;
  display: inline;
  color: var(--color-text-light);
  line-height: 1.6;
}

/* Full-width tables */
.article-body .table-wrapper {
  width: 100%;
  overflow-x: auto;
  margin-bottom: 2em;
}
.article-body .table-wrapper table {
  width: 100%;
  border-collapse: collapse;
}
.article-body .table-wrapper th,
.article-body .table-wrapper td {
  padding: 0.75em 1em;
  text-align: left;
  border-bottom: 1px solid var(--color-border);
}
.article-body .table-wrapper thead th {
  font-weight: 600;
  color: var(--color-text);
  border-bottom: 2px solid var(--color-border);
}

@media (max-width: 900px) {
  .article-hero {
    min-height: 280px;
  }

  .article-hero-content {
    min-height: 280px;
  }

  .article-header {
    padding: 1.5rem;
    text-align: center;
  }

  .article-meta {
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .article-header h1 {
    font-size: 1.8rem;
  }

  .article-description {
    font-size: 1rem;
  }
}
</style>

